import React, { Fragment } from 'react';
import { Header } from './components';
import './App.css';

const App = (props) => (
  <Fragment>
    <Header />
    <div>Footer</div>
  </Fragment>
);

export default App;
